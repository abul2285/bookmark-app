import styled from 'styled-components';
import Login, { LoginWrap } from '../Login/Login.stc';

export const SignUpWrap = styled(LoginWrap)``;

export const DorikStyle = styled.div`
    width: 94px;
    height: 40px;
    margin-bottom: 40px;
`;

const SignUp = styled(Login)``;

export default SignUp;
